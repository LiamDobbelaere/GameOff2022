using UnityEngine;

public class DocumentarySceneOptions : MonoBehaviour {
    public AudioClip music;
    public float musicVolume = 0.5f;

    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
